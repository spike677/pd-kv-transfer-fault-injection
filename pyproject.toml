[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pd-kv-fault"
version = "0.2.0"
description = "Isolated developer-only Mooncake return-code fault emulator"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
pd-kv-fault = "pd_kv_fault.cli:main"

[tool.setuptools]
packages = ["pd_kv_fault"]
